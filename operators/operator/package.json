{
  "name": "@atls/k8s-operator",
  "version": "0.0.1",
  "license": "BSD-3-Clause",
  "main": "src/index.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "yarn app library build",
    "postpack": "rm -rf dist",
    "prepack": "yarn run build"
  },
  "dependencies": {
    "@atls/logger": "^0.0.1",
    "@kubernetes/client-node": "^0.15.1",
    "async": "^3.2.1",
    "axios": "^0.21.4"
  }
}
